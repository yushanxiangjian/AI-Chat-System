server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_chat?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root

  redis:
    host: localhost
    port: 6379

openai:
  api:
    key: YOUR_DEEPSEEK_API_KEY_HERE
